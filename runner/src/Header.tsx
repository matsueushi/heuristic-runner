import { AppBar, Toolbar } from "@mui/material";

function Header() {
  return (
    <header>
      <AppBar>
        <Toolbar>Heuristic Runner</Toolbar>
      </AppBar>
    </header>
  );
}

export default Header;
